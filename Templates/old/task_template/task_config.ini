[Arduino]
com_port = /dev/ttyACM0:Arduino Mega
baud_rate = 9600
var_fname = init_variables.h # replace this with the filename of the variables that are exposed to the interface
event_codes_fname = event_codes_fname.h # replace this with the filename of the event codes
pio_project_folder = Arduino

